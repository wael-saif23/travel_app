import 'package:flutter/material.dart';

class CanceledView extends StatelessWidget {
  const CanceledView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Canceled View"),
    );
  }
}